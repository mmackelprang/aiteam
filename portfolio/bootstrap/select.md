# Repo selection — bootstrap worksheet

Generated/updated by `enumerate.py` (Task 1: raw repo list) and
`filter_defaults.py` (Task 2: default excludes applied, checklist form).

The human marks include/exclude here, and the pilot set of 3–5 repos is
confirmed with the user **before** `ingest.py` (Task 3) runs against them.

_Nothing enumerated yet — run Task 1 first._
