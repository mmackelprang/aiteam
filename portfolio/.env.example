# Copy to .env (gitignored). Never commit real values.
GITHUB_TOKEN=            # PAT with read access to the tracked org/personal repos
OBSIDIAN_API_KEY=        # from Obsidian's Local REST API plugin settings
OBSIDIAN_VAULT_PATH=     # filesystem fallback only, if the REST API is ever unavailable
