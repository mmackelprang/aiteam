"""Shared helpers for the portfolio subtree (never imported from ../harness).

One concern per module:
  frontmatter.py — byte-preserving frontmatter surgery (the F6 splice engine)
  vault.py       — note discovery, atomic writes, dashboard marker blocks
  confidence.py  — signal table, confidence scoring, activity_state
  github_api.py  — GitHub data acquisition (REST; fixture source for offline)
"""
