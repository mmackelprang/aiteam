# Copy to .env (gitignored). Never commit real values.
# Per-agent Anthropic auth is referenced via Paperclip secret syntax
# (${secrets.anthropic_key}) in adapter config — see HANDOFF §4.3.
ANTHROPIC_API_KEY=
SLACK_BOT_TOKEN=
SLACK_SIGNING_SECRET=
GOOGLE_CHAT_WEBHOOK_URL=   # per space; referenced from config/notification_routing/<project>.yaml
